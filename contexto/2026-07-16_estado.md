# Estado do Projeto — Conversor PDF → DOCX

**Data:** 2026-07-16
**Versão consolidada:** v3.1.0 (DocLayout-YOLO + otimização de CPU e cache configurável)
**Repositório:** https://github.com/PatrickSouzaL/ConversorPdfDocx (branch `main`; v3.0.0 = commit `71f462a`)
**Status:** v3.0.0 validada com sucesso pelo usuário; v3.1.0 aplicada, pendente de validação end-to-end (Python 3.10–3.12) e merge.

---

## 0. Contexto crítico

Os PDFs de origem são **exportações "chapadas"**: páginas renderizadas inteiramente
como imagem, misturando **blocos de texto instrucional** e **prints de tela**. A
regra de negócio exige **texto nativo e editável no Word** (não imagem, não camada
oculta). A v3.0.0 mantém o pipeline de reconstrução do DOCX do zero, mas **troca a
análise de layout heurística (v2.0.0) por um modelo de Visão Computacional**.

---

## 1. Lógica / Código atual

Pipeline em 3 passos, por arquivo:

- **Passo 1 — Rasterização** (`conversor/rendering.py`): `pdf2image`/Poppler
  converte cada página em imagem BGR de alta resolução (300 dpi), uma por vez.
- **Passo 2 — Layout Analysis por ML** (`conversor/detector.py` + `layout.py` + `ocr.py`):
  - `detector.detect_regions()`: roda o **DocLayout-YOLO** na página e devolve os
    bounding boxes com as classes de layout, já mapeadas para **TEXTO / IMAGEM**;
  - `layout.analyze_page()`: para cada região TEXTO faz OCR no recorte, para IMAGEM
    guarda o bbox, e ordena os blocos no fluxo de leitura (top-to-bottom, esq→dir).
    Região de TEXTO com OCR vazio vira IMAGEM (preserva conteúdo);
  - `ocr.ocr_region_text()`: pré-processamento OpenCV (cinza + inversão de fundo
    escuro + Otsu) aplicado **só no recorte** de texto; OCR com PSM 6.
- **Passo 3 — Reconstrução** (`conversor/docx_builder.py`): `python-docx` remonta o
  DOCX na ordem de leitura, com parágrafos de texto nativo e imagens recortadas da
  **imagem original colorida**; quebra de página entre páginas.

### Modelo de IA

- **DocLayout-YOLO** — `juliozhao/DocLayout-YOLO-DocStructBench` (YOLOv10, pacote
  `doclayout_yolo`). Pesos **baixados automaticamente** do HuggingFace no 1º uso e
  cacheados.
- **Hardware (fallback automático):** `resolve_device()` usa `torch` para detectar
  GPU CUDA; havendo, roda nela, senão CPU. Registrado no log: `Hardware de inferência: ...`.
- **Mapeamento de classes → domínios** (`config.py`): `TEXT_CLASSES` (title,
  plain text, *_caption, table_footnote, …) → TEXTO; `IMAGE_CLASSES` (figure, table,
  isolate_formula, …) → IMAGEM; `DISCARD_CLASSES` (`abandon`) → descartada; classe
  desconhecida → IMAGEM (não perde conteúdo).
- **Multiprocessamento seguro:** pesos baixados **uma vez** no processo principal
  (`ensure_weights()`, sem corrida entre workers); modelo carregado **uma vez por
  worker** (singleton lazy `_MODEL` em `detector.py`), nunca por arquivo. Em GPU, o
  padrão é **1 worker** (evita OOM de VRAM).

### Arquitetura modular (pacote `conversor/`)

| Módulo | Responsabilidade |
| --- | --- |
| `config.py` | Dataclasses picláveis (`PipelineConfig`, `Block`, `ConversionResult`) + mapeamento de classes. |
| `dependencies.py` | Detecção de Poppler, idiomas do Tesseract e das dependências de IA (`ml_dependencies_available`). |
| `rendering.py` | Passo 1: rasterização das páginas. |
| `detector.py` | Passo 2 (IA): `resolve_device`, `ensure_weights`, singleton do modelo, `detect_regions`. |
| `ocr.py` | Pré-processamento + OCR por região (`ocr_region_text`) + `TESSDATA_PREFIX`. |
| `layout.py` | Passo 2: orquestra detecção + OCR das regiões + ordem de leitura (`analyze_page`). |
| `docx_builder.py` | Passo 3: reconstrução do DOCX (`DocxBuilder`). |
| `pipeline.py` | `convert_single` — orquestra os 3 passos (worker, fail-safe). |
| `main.py` | CLI, `discover_pdfs`, `_plan_jobs`, `run_batch`, resolução de hardware/pesos, `main`. |

### CLI

```powershell
python main.py                     # ./pdf -> ./docx (GPU se houver, senão CPU)
python main.py --dpi 400 --verbose
python main.py --ocr-lang eng
python main.py --workers 4 --force
python main.py --device cpu        # força CPU mesmo com GPU disponível
```

Flags: `--src`, `--dst`, `--workers`, `--force`, `--dpi` (300), `--ocr-lang`
(`por+eng`), `--min-conf` (40), `--device` (`auto`), `--yolo-conf` (0.2),
`--yolo-imgsz` (1024), `--verbose`. Exit code `1` se houver falha; `2` se Poppler,
Tesseract ou dependências de IA estiverem ausentes, ou se o download dos pesos falhar.

### Stack

- **Python 3.10–3.12 recomendado** (faixa com wheels estáveis de `torch`/`doclayout-yolo`;
  em 3.13/3.14 pode não haver wheel).
- Python deps (`requirements.txt`): `pdf2image==1.17.0`, `doclayout-yolo>=0.0.3`,
  `huggingface_hub>=0.24`, `torch`, `pytesseract==0.3.13`, `python-docx==1.2.0`,
  `opencv-python`, `numpy`, `Pillow`.
- **Binários de sistema:** Poppler (pdf2image) e Tesseract OCR (pytesseract),
  auto-detectados (`.poppler/`, `.tessdata/`).
- **GPU (opcional):** NVIDIA/CUDA acelera a inferência; instalar o wheel CUDA do
  PyTorch antes do `requirements.txt`.

### Adições da v3.1.0 (otimização de CPU e cache)

- **Threads do torch por worker** (`PipelineConfig.cpu_threads`): em CPU, o `main`
  calcula `núcleos ÷ workers` e cada worker chama `torch.set_num_threads()` (em
  `detector._get_model`), eliminando o *oversubscription* do ProcessPool. Ajustável
  via `--workers` (menos workers = mais threads/worker).
- **`.env` carregado** (`main.load_project_env`, via `python-dotenv`): `HF_HOME` e
  `YOLO_CONFIG_DIR` passam a valer (cache dos modelos fora do AppData) e são herdados
  pelos workers. O `.gitignore` passa a excluir `.models_cache/`.
- **Tempo por página** logado: por arquivo (`Xs/pág.`) e média global no resumo
  (`soma do tempo de processamento ÷ total de páginas`), para benchmark de CPU.
- **`half`/FP16 não usado em CPU** (é ganho de GPU; em CPU costuma ser neutro/pior).

---

## 2. Decisões técnicas firmadas nesta sessão

1. **DocLayout-YOLO em vez de `yolov8n.pt`**: o `yolov8n.pt` é treinado no COCO (80
   objetos do dia-a-dia) e **não detecta regiões de documento** — o mapeamento
   TEXTO/IMAGEM seria impossível. Escolhido o `DocStructBench` (YOLOv10 ajustado em
   documentos), que mantém intacto o resto do spec (ultralytics, download automático,
   fallback de hardware).
2. **Removida toda a heurística da v2.0.0**: projeção por bandas, `colorfulness`
   (Hasler), densidade de tinta, proporção alfabética, morfologia OpenCV e os limiares
   correspondentes (`color_threshold`, `min_words`, `fill_threshold`, etc.).
3. **Concorrência:** `ProcessPoolExecutor` (isolamento de falhas) com modelo carregado
   **1x por worker** (singleton lazy); pesos pré-baixados no processo principal; **GPU
   limitada a 1 worker** por padrão (VRAM).
4. **OCR por região**, não por página inteira; pré-processamento Otsu só no recorte de
   texto; imagem original colorida preservada para os prints.
5. **Preservação de conteúdo:** classe desconhecida e texto com OCR vazio viram IMAGEM.
6. **YOLO26 avaliado e recusado (v3.1.0):** o `yolo26n.pt` é COCO (não detecta layout
   de documento); trocá-lo quebraria o DLA. A aceleração de CPU veio de threads/cache,
   sem trocar o modelo. Um YOLO26 *fine-tuned* em documentos (DocLayNet) fica como
   opção futura (exige treino).
7. **Otimização de CPU por threads** (v3.1.0): resolver o oversubscription do
   ProcessPool é o maior ganho de CPU; `half`/FP16 é ganho de GPU, não de CPU.
8. **Mantidos:** pacote modular `conversor/`, fail-safe por processo, idempotência de
   nomes, filtro estrito `.pdf`, logging UTF-8, commits sem *Co-Authored-By*.

---

### Base de Conhecimento (`base_conhecimento.json`)

Refatoração e higienização completa do arquivo de suporte técnico, contendo 136 procedimentos de TI de sistemas (Windows, Microsoft 365, Google Workspace, redes, impressoras, assinadores, etc.):
- **Esquema Estrito:** Padronização absoluta de todos os itens com as chaves `"Titulo"`, `"Descricao"`, `"Resolucao"` e `"Memorando"`.
- **Limpeza de OCR:** Remoção de ruídos de captura, caracteres corrompidos, e trechos de texto duplicados provenientes do OCR original.
- **Enriquecimento Técnico:** Estruturação em passos lógicos numerados e adição de notas e alertas críticos de infraestrutura (como impacto de licenças, regras de firewall e dependências de drivers) na chave `"Memorando"`.

---

## 2. Decisões técnicas firmadas nesta sessão

1. **DocLayout-YOLO em vez de `yolov8n.pt`**: o `yolov8n.pt` é treinado no COCO (80 objetos do dia-a-dia) e **não detecta regiões de documento** — o mapeamento TEXTO/IMAGEM seria impossível. Escolhido o `DocStructBench` (YOLOv10 ajustado em documentos), que mantém intacto o resto do spec (ultralytics, download automático, fallback de hardware).
2. **Removida toda a heurística da v2.0.0**: projeção por bandas, `colorfulness` (Hasler), densidade de tinta, proporção alfabética, morfologia OpenCV e os limiares correspondentes (`color_threshold`, `min_words`, `fill_threshold`, etc.).
3. **Concorrência:** `ProcessPoolExecutor` (isolamento de falhas) com modelo carregado **1x por worker** (singleton lazy); pesos pré-baixados no processo principal; **GPU limitada a 1 worker** por padrão (VRAM).
4. **OCR por região**, não por página inteira; pré-processamento Otsu só no recorte de texto; imagem original colorida preservada para os prints.
5. **Preservação de conteúdo:** classe desconhecida e texto com OCR vazio viram IMAGEM.
6. **YOLO26 avaliado e recusado (v3.1.0):** o `yolo26n.pt` é COCO (não detecta layout de documento); trocá-lo quebraria o DLA. A aceleração de CPU veio de threads/cache, sem trocar o modelo. Um YOLO26 *fine-tuned* em documentos (DocLayNet) fica como opção futura (exige treino).
7. **Otimização de CPU por threads** (v3.1.0): resolver o oversubscription do ProcessPool é o maior ganho de CPU; `half`/FP16 é ganho de GPU, não de CPU.
8. **Refatoração da Base de Conhecimento (`base_conhecimento.json`):** A base legada foi limpa em lote e estruturada em JSON estrito (136 itens), sanando os erros gramaticais e de OCR, e melhorando os roteiros de suporte do N1/N2.
9. **Mantidos:** pacote modular `conversor/`, fail-safe por processo, idempotência de nomes, filtro estrito `.pdf`, logging UTF-8, commits sem *Co-Authored-By*.

---

## 3. Próximos passos (exatos)

1. **Validar a v3.1.0 num piloto** em venv **Python 3.10–3.12**: `pip install -r requirements.txt` e `python main.py --verbose` com **3–5 PDFs** em `pdf/`. Conferir o log `Xs/página`, a qualidade (texto editável + prints) e o cache indo p/ `.models_cache/`. *Ambiente atual é Python 3.14.6 sem torch — validação de IA não roda aqui.*
2. **Conversão em massa (dezenas de arquivos):** só depois do piloto OK. Fail-safe + idempotência já suportam lote grande e retomada. **Atenção a RAM**: cada worker carrega uma cópia do modelo — em máquina de 12 núcleos, prefira `--workers 4` (limita RAM e dá mais threads/worker) a deixar 12 workers. Rodar `--force` só quando quiser reprocessar.
3. **Ajuste da detecção com PDFs reais**: calibrar `--yolo-conf`/`--yolo-imgsz` e revisar o mapeamento de classes em `config.py` se surgirem falsos positivos/negativos.
4. **Qualidade/ordem de leitura**: avaliar `--dpi 400` e `--yolo-imgsz 1280` para texto pequeno; revisar ordenação em layouts multicoluna complexos.
5. **Testes automatizados (`pytest`)**: cobrir `discover_pdfs`, `_plan_jobs` (idempotência), `detector._map_kind` (mapeamento de classes) e `resolve_workers` (cap de GPU) com mocks do modelo.
6. **Manutenção da Base de Conhecimento:** Validar o arquivo `base_conhecimento.json` em produção. Utilizar as resoluções higienizadas como fonte de dados para o sistema de suporte técnico.
7. **Futuro:** YOLO26 *fine-tuned* em DocLayNet (velocidade sem quebrar o DLA); relatório de saída (JSON/CSV) e modo `--watch` (backlog).

