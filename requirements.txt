# Conversor PDF -> DOCX por Document Layout Analysis (ML / DocLayout-YOLO)

# Rasterização
pdf2image==1.17.0

# Inteligência Artificial (Visão Computacional / detecção de layout)
# DocLayout-YOLO = YOLOv10 ajustado em documentos. Vendoriza o ultralytics no
# próprio namespace (não instale ultralytics separadamente, para evitar conflito).
doclayout-yolo>=0.0.3
huggingface_hub>=0.24
# torch é puxado pelo doclayout-yolo. Em CPU o wheel padrão basta; para GPU,
# instale antes o wheel CUDA apropriado (ver https://pytorch.org/get-started).
torch

# Configuração de ambiente (.env: HF_HOME / YOLO_CONFIG_DIR p/ cache dos modelos)
python-dotenv>=1.0

# OCR
pytesseract==0.3.13

# Word
python-docx==1.2.0

# Tratamento de imagens e arrays
opencv-python
numpy
Pillow
